import 'package:flutter/material.dart';

const primaryColor = Color(0xffffc107);
const secondaryColor = Color(0xff242430);
const darkColor = Color(0xff191923);
const bodyTextColor = Color(0xff888bbd);
const bgColor = Color(0xff1e1e28);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // for animation
const maximumWidth = 1440.0; // maximum width for web